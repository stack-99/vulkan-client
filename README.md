# VulkanClient

## Prequisets
- Download CMAKE, VulkanSDK 1.2.176, glfw
- Run the Vulkan Hardware Capability Viewer 
- The environment variable VK_SDK_PATH and VULKAN_SDK should be automatically set to your Vulkan SDK

## How to create a VS Solution
- Create a folder named build
- Inside of the build folder run cmake ..